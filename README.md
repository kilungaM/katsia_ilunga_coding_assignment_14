# Ilunga Katsia – Coding Assignment 14

This project is a Docker-hosted version of my personal portfolio website built using React. The application is fully containerized using a two-stage Docker build and runs on the required port **5575** as per the assignment requirements.

## Features

- Responsive React portfolio website
- Production build served through Nginx
- Docker container running on the assigned port
- Files hosted in a working directory following the required naming format:

- Local development supported with `npm start`

## Technologies Used

- **React (create-react-app)**
- **Node.js**
- **Nginx**
- **Docker / Docker Desktop**

## Key Files

## Docker Instructions

### 1️ Build the Docker Image

Make sure you are inside the project folder, then run:

```bash
docker build -t ilunga_katsia_assignment_14 .

```
